/* Problem Statement:
   Write a C program to demonstrate relational operators in C.
*/
#include <stdio.h>
int main() 
{
    int a = 10, b = 20;
    printf("%d > %d = %d\n", a, b, a > b);
    printf("%d < %d = %d\n", a, b, a < b);
    return 0;
}
